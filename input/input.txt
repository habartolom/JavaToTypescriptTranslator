public class Animal<T extends algo>  implements a, b, c{
  public private void animalSound(); // interface method (does not have a body)
  private void run(); // interface method (does not have a body)
  public int getSum(int x, int y){
    return x + y;
  }
}